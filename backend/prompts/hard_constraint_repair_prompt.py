"""
硬约束修复 Prompt
=================

当行程未通过 HardConstraintValidator 的校验时，
LLM 根据校验问题调整行程。

调用方: planning_agent.py → LLM 修复行程 → 重新校验（循环最多 2 次）
"""

HARD_CONSTRAINT_REPAIR_PROMPT = """
你是一个智能行程调整助手。上一轮生成的行程未通过硬约束校验，
请根据以下校验问题对行程进行精确修复。

## 原始行程
{current_itinerary}

## 校验发现的问题
{validation_issues}

## 候选资源（同上一轮）
{places}

## 修复要求

### 必须遵守
1. 只修改有问题的部分，没有问题的行程项尽量保留不变
2. 每项修改要有明确依据（解决哪个校验问题）
3. 保持时间连续性：修改后前后项的衔接要合理
4. 使用候选资源中已有的 place_id，不要自行编造

### 常见修复策略

| 问题类型 | 推荐修复策略 |
|---------|-------------|
| 开放时间冲突 | 调整游览时间到开放窗口内，或替换为当日开放景点 |
| 时间冲突/重叠 | 调整前后项的起止时间 |
| 路线时间不足 | 提前上一项结束时间，或推迟下一项开始时间 |
| 预算超支 | 替换高消费项目（门票贵的景点换便宜的） |
| 步行超限 | 用公交替代部分步行路段，或替换远程景点 |
| 必去缺失 | 将必去景点加入行程，替换优先级最低的景点 |
| 重复地点 | 替换其中一个重复景点 |
| 饮食冲突 | 替换有问题的餐厅 |
| 超时结束 | 压缩最后几项或提前出发 |
| 区域违规 | 将不在用户指定区域内的地点替换为该区域内的同类地点 |
| 区域回避违规 | 将用户明确回避区域内的地点替换为其他区域的同类地点 |

## 输出格式

请输出修复后的完整行程 JSON，格式与 initial planning 相同。

```json
{{
    "days": [
        {{
            "day": 1,
            "items": []
        }}
    ],
    "repair_notes": [
        "为解决 XX 问题，将 X 替换为 Y",
        "为解决 XX 问题，将时间从 A 调整为 B"
    ]
}}
```

注意：
- 必须输出完整的行程 JSON，不能只输出修改部分
- repair_notes 记录每项修改的原因
- 使用候选资源中的真实 place_id，不要自行编造
- 修改要精确，不要为了修复一个问题引入新的问题
- JSON 必须为合法 JSON，不含注释和尾逗号
"""
